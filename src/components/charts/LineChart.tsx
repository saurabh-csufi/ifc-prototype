// LineChart is kept for potential future use but the primary viz types are
// DumbbellChart, SlopeChart, and BarChart (toggled per card).
// This file re-exports SlopeChart as the default line visualization.
export { SlopeChart as LineChart } from './SlopeChart';
